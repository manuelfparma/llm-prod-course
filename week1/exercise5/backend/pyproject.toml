[project]
name = "backend"
version = "0.1.0"
description = "EASY-CHATGPT — FastAPI backend proxy for LLM chat"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.138.0",
    "openai>=2.43.0",
    "python-dotenv>=1.2.2",
    "sse-starlette>=3.4.5",
    "uvicorn[standard]>=0.49.0",
]
